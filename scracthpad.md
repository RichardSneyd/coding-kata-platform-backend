# Scracthpad - ideas

Deployment platform - Firebase?

Firebase Authentication. Build microservices to communicate with each other as needed. 
Firestore is a NoSQL DB service. Could use this?

// Firebase, Netlify do NOT support dynamic hosting (like Java, Node, PHP etc). Only serverless functions.
// Linux hosting environment

// Possible free hosts for compile API (Must support Java & Node):
Google App Engine
RedHat OpenShift


// should we speak with the tech team about utilizing their existing hosting account in some way?

// AWS free:
DB: 25 GB of  NoSQL DynamoDB storage
200M requests p/m
Compute: 1 Million AWS Lambda requests p/m (3.1 millions seconds p/m)
Content delivery: 1 TB p/m

// 12 month trial
EC2 (VM) 750 hrs p/m
S3 5 GB 

// Google Cloud
Compute Engine

// android app?
navigaton, backStack 