# config environment
environment=development
DOMAIN_NAME=http://localhost
API_PORT=8443
FRONTEND_PORT=3000

#paths
node.path=/Users/richardsneyd/.nvm/versions/node/v14.21.3/bin
storage.images.headshots=./data/images/headshots
storage.pdfs.resumes=./data/pdfs/resumes

# config server port
server.port=8080

server.tomcat.max-threads=10

# Datasource properties
spring.datasource.url=jdbc:postgresql://localhost:5432/lms_db
spring.datasource.username=
spring.datasource.password=

spring.datasource.continue-on-error=true
#spring.main.allow-circular-references=true

# db connection pooling for memory efficiency
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=3
spring.datasource.hikari.idle-timeout=300000

# batching for memory conservation
spring.jpa.properties.hibernate.jdbc.batch_size=20

# Use caching
spring.jpa.properties.hibernate.cache.use_second_level_cache=true
spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.ehcache.EhCacheRegionFactory

spring.jackson.serialization.fail-on-empty-beans=false

server.compression.enabled=true
server.compression.min-response-size=1024

# Hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQL94Dialect
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.show-sql=false
spring.jpa.show-sql=false
spring.jpa.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# Spring security properties
spring.security.user.name=admin
spring.security.user.password=password1

# Email Config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=noreply.brightcode@gmail.com
spring.mail.password=ubntatljicsuskrx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true



